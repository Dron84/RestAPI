const auth = require('../controllers/auth')

module.exports = (app)=>{



    // /file/upload post
    // /file/list [GET]
    // /file/delete/:id delete
    // /file/:id[GET]
    // /file/download/:id[GET]
    // /file/update/:id[put]


    // /info [GET]
    // /logout [GET]
}